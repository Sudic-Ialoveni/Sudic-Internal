{
  "name": "sudic-internal",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "install:all": "npm install && (cd frontend && npm ci)",
    "fly:deploy": "fly deploy ./backend --depot=false"
  },
  "dependencies": {
    "sudic-internal-backend": "file:./backend"
  }
}
